已实现
实时获取定位信息并在屏幕上显示。

相较于前面几版优化了代码，精简了

这版修复了画线起止点x/y坐标相同导致的重启问题。

新增：
当绘制的点超过屏幕，则会更新偏移值的最大范围scale_max，并重新绘制之前的所有点位。

缺点：
1. 重新绘制只能一次，第二次不知道为什么会失败
2. 重新绘制只画点，没有画线

后续增加：
1. 将信息（时间、WGS经纬坐标、百度坐标，lcd坐标）存到sd卡中
2. 尝试解决模块空间不足的报错
3. 使用esp32向云服务发送信息